plugins {
    id "java-library"
    id "signing"
    id "maven-publish"
}

description = "RSocket-rpc-virtualthreads api library"

compileJava {
    options.compilerArgs << "--enable-preview"
}

dependencies {
    api project(":rsocket-rpc-futures")
}